name: Update Licenses

on:
  push:
    branches:
      - main
    paths:
      - '**/build.gradle.kts'
      - 'gradle/libs.versions.toml'
      - 'settings.gradle.kts'

permissions:
  contents: write
  pull-requests: write

jobs:
  update-licenses:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup Java and Gradle
        uses: ./.github/actions/setup-java-gradle
      - name: 🤖 ExportLibraryDefinitions for android
        run: ./gradlew :app:androidApp:exportLibraryDefinitions --no-configure-on-demand
        env:
          ORG_GRADLE_PROJECT_githubApiToken: ${{ secrets.GITHUB_TOKEN }}
      - name: 🖥️ ExportLibraryDefinitions for jvm
        run: ./gradlew :app:jvmApp:exportLibraryDefinitions --no-configure-on-demand
        env:
          ORG_GRADLE_PROJECT_githubApiToken: ${{ secrets.GITHUB_TOKEN }}
      - name: 🍎 ExportLibraryDefinitions for ios
        run: ./gradlew :app:ios:exportLibraryDefinitions --no-configure-on-demand
        env:
          ORG_GRADLE_PROJECT_githubApiToken: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8
        with:
          token: ${{ secrets.PAT }}
          title: 'Update aboutlibraries.json'
          body: 'This PR updates the aboutlibraries.json'
          base: main
          branch: chore/update-licenses
          labels: ':robot:create-pull-request'
          commit-message: 'Update aboutlibraries.json'

      - name: Enable Auto Merge
        if: steps.cpr.outputs.pull-request-operation == 'created'
        uses: peter-evans/enable-pull-request-automerge@a660677d5469627102a1c1e11409dd063606628d # v3
        with:
          token: ${{ secrets.PAT }}
          pull-request-number: ${{ steps.cpr.outputs.pull-request-number }}
          merge-method: squash
