name: Internal App Sharing

on:
  workflow_dispatch:
  pull_request:
    branches:
      - 'main'
  push:
    branches:
      - main
    paths-ignore:
      - '.idea/**'
      - '.run/**'
      - 'config/**'
      - 'docs/**'
      - 'iosApp/**'
permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: SorrowBlue/github-actions/.github/workflows/android-publish.yml@main
    secrets: inherit
    with:
      environment: android
      fetch-depth: 0
      task: bundleInternal
      package-name: com.sorrowblue.comicviewer
      release-files: app/android/build/outputs/bundle/internal/android-internal.aab
      track: internalsharing
      mapping-file: app/android/build/outputs/mapping/internal/mapping.txt
      debug-symbols: app/android/build/intermediates/merged_native_libs/internal/mergeInternalNativeLibs/out/lib


#  build_and_upload:
#    runs-on: ubuntu-latest
#    environment: android
#    steps:
#      - name: Checkout
#        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
#        with:
#          fetch-depth: 0
#      - name: Setup Java and Gradle
#        uses: SorrowBlue/github-actions/.github/actions/setup-java-gradle@main
#      - name: Decode android keystore file
#        id: android_keystore
#        uses: timheuer/base64-to-file@v2
#        with:
#          fileName: 'android_keystore.jks'
#          encodedString: ${{ secrets.ANDROID_STORE_FILE_BASE64 }}
#      - name: Run bundleInternal
#        run: ./gradlew bundleInternal
#        env:
#          ORG_GRADLE_PROJECT_androidSigningReleaseStoreFile: ${{ steps.android_keystore.outputs.filePath }}
#          ORG_GRADLE_PROJECT_androidSigningReleaseStorePassword: ${{ secrets.ANDROID_STORE_PASSWORD }}
#          ORG_GRADLE_PROJECT_androidSigningReleaseKeyAlias: ${{ secrets.ANDROID_KEY_ALIAS }}
#          ORG_GRADLE_PROJECT_androidSigningReleaseKeyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
#        with:
#          serviceAccountJson: ${{ steps.auth.outputs.credentials_file_path }}
#          packageName: com.sorrowblue.comicviewer
#          releaseFiles: app/android/build/outputs/bundle/internal/android-internal.aab
#          track: internalsharing
#          mappingFile: app/android/build/outputs/mapping/internal/mapping.txt
#          debugSymbols: app/android/build/intermediates/merged_native_libs/internal/mergeInternalNativeLibs/out/lib
#      - name: Notify Discord
#        uses: sarisia/actions-status-discord@v1
#        with:
#          webhook: ${{ secrets.DISCORD_WEBHOOK }}
#          nodetail: true
#          title: ":sparkles: Uploaded to internal app share."
#          description: |
#            Click [here](${{ steps.upload.outputs.internalSharingDownloadUrl }}) to download!
#          color: 0xff91a4
