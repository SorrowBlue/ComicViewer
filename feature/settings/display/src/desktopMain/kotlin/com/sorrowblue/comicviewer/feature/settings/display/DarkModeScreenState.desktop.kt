package com.sorrowblue.comicviewer.feature.settings.display

import com.sorrowblue.comicviewer.domain.model.settings.DarkMode

/**
 * No implementation needed for desktop platform as dark mode is handled in the theme [com.sorrowblue.comicviewer.framework.designsystem.theme.colorScheme] function.
 */
internal actual fun updateDarkMode(darkMode: DarkMode) {
    // No implementation needed for desktop platform
}
